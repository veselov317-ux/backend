spring:
  datasource:
    url: jdbc:h2:mem:sysdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
    driver-class-name: org.h2.Driver
  data:
    redis:
      host: localhost
      port: 6379
  cache:
    type: simple

app:
  jwt:
    secret: service-desk-test-secret-key-change-me-please-32
    expiration-ms: 86400000
